# IronCore — Python Dependencies
# Install with: pip install -r requirements.txt

# ─── Core Framework ───────────────────────────────────────────
asyncio          # stdlib — event loop (no install needed)
pydantic>=2.0    # Data validation and typed schemas

# ─── Security ─────────────────────────────────────────────────
cryptography     # SecretsVault: Fernet AES-128-CBC encryption

# ─── Sandbox Isolation ────────────────────────────────────────
docker           # Docker SDK — ephemeral container management

# ─── Browser / Stealth Layer ──────────────────────────────────
playwright       # Headless browser automation
playwright-stealth  # Additional bot fingerprint evasion patches
opencv-python    # GeeTest Slider CAPTCHA — Template/Edge matching
numpy            # Bezier curve trajectory math

# ─── Memory & Knowledge Graph ─────────────────────────────────
chromadb         # Vector store for semantic memory (RAG)
networkx         # Local knowledge graph (entity relationships)
requests         # HTTP client — Ollama/VLM REST API calls

# ─── LLM Integration ──────────────────────────────────────────
litellm          # Unified API for OpenAI / Anthropic / local models

# ─── Testing ──────────────────────────────────────────────────
pytest
pytest-asyncio
aiosqlite         # SessionStore: async SQLite driver

# ─── V2 Optimizer (Phase 1) ───────────────────────────────────
sentence-transformers  # SemanticCache: all-MiniLM-L6-v2 embedding model
